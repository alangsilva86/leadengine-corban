/** @vitest-environment jsdom */
import { describe, it, expect } from 'vitest';

describe('WhatsAppConnect placeholder suite', () => {
  it('runs basic sanity check', () => {
    expect(true).toBe(true);
  });
});
