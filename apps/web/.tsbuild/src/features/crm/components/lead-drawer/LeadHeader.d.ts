import type { LeadDetail } from '../../state/leads';
type LeadHeaderProps = {
    lead: LeadDetail;
};
declare const LeadHeader: ({ lead }: LeadHeaderProps) => import("react/jsx-runtime").JSX.Element;
export default LeadHeader;
