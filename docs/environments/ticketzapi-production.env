# Ticketz API - Produção (Railway)
# Atualize estas variáveis na instância `ticketzapi-production` para alinhar com o CORS da API.
# Domínios adicionais devem ser separados por vírgula.

# Origem principal do frontend (Railway)
FRONTEND_URL=https://leadengine-corban.up.railway.app

# Domínios extras liberados pelo CORS
CORS_ALLOWED_ORIGINS=https://leadengine-corban.up.railway.app

# Feature flags compartilhados
# Ajuste para `true` quando quiser expor rotas de debug do WhatsApp.
FEATURE_DEBUG_WHATSAPP=false

# Banco e autenticação
DATABASE_URL=postgresql://ticketz:ticketz123@postgres:5432/ticketz
JWT_SECRET=defina-um-segredo

# Onboarding e convites
# A API expõe `/api/onboarding/invitations` para emitir/listar convites autenticados.
# Esses valores são utilizados pelo serviço `onboarding-invites-service` para montar o link
# `${ONBOARDING_PORTAL_BASE_URL}?token=<TOKEN>` e preencher os dados do envio (e-mail/SMS).
ONBOARDING_INVITE_EMAIL_FROM=onboarding@ticketz.com.br
ONBOARDING_INVITE_SMS_SENDER=Ticketz
ONBOARDING_PORTAL_BASE_URL=https://leadengine-corban.up.railway.app/onboarding
POLL_METADATA_ENCRYPTION_KEY=defina-um-segredo-exclusivo-para-enquetes
