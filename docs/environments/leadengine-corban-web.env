# LeadEngine Web (Railway)
# Configuração de variáveis para habilitar o proxy interno da aplicação web.

# URL da API real utilizada pelo proxy do frontend.
# Mantemos o mesmo valor usado anteriormente em VITE_API_URL para evitar CORS.
# O servidor agora sanitiza o caminho antes de encaminhar as requisições, evitando
# duplicar segmentos como /api/.../api/..., mas o valor recomendado continua sendo
# apenas a origem da API.
VITE_API_URL=https://ticketzapi-production.up.railway.app

# Opcional: caso queira separar a configuração do proxy do valor exposto ao bundle,
# defina explicitamente a origem da API. Se vazio, o servidor reutiliza VITE_API_URL.
# Assim como acima, mantenha a origem — com ou sem caminho base — e deixe o proxy
# cuidar da normalização dos caminhos.
#API_PROXY_TARGET=https://ticketzapi-production.up.railway.app

# Hosts adicionais que devem sempre usar o modo same-origin (vírgula separados).
# O host de produção já é incluído automaticamente.
#VITE_API_SAME_ORIGIN_HOSTS=leadengine-corban.up.railway.app

# Observação: a API pareada com este frontend deve manter WHATSAPP_PASSTHROUGH_MODE=false
# para continuar validando x-api-key/x-signature em todos os webhooks. Os fluxos de
# alocação de leads e exportações no frontend consultam a API por instanceId, então
# garanta que as instâncias autorizadas estejam devidamente cadastradas.
